class Product {
  String name;      // Назва 
  int count;        // Кількість
  double price;     // Ціна

  Product({
    required this.name,
    required this.count,
    required this.price,
  });
}